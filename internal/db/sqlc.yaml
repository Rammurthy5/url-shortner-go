version: "2"
sql:
  - schema: "./schema"
    queries: "./queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "./sqlc"
        sql_package: "pgx/v5"
    database:
      managed: true
    rules:
      - sqlc/db-prepare
